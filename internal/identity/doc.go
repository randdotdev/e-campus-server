// Package identity owns who a person is on the platform: user accounts and
// credentials, login sessions and refresh-token rotation, institutional role
// assignment, staff profiles, and per-user UI preferences. It defines
// entities, pure rules, ports, and the application services, and depends on
// no infrastructure.
package identity
